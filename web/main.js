function regPush() {
	var data_pushups = document.getElementById("data_pushups").value
	eel.regPush(data_pushups)
}

function regCurl() {
	var data_curls = document.getElementById("data_curls").value
	eel.regCurl(data_curls)
}

function regSit() {
	var data_situps = document.getElementById("data_situps").value
	eel.regSit(data_situps)
}
